import { Styles } from 'dripsy';

const styles = Styles.create({
  label: {
    marginBottom: 8,
  },
  required: {
    color: '#DB3131',
  },
  container: {
    alignItems: 'center',
    borderColor: '#8156FF',
    borderRadius: 8,
    borderWidth: 1,
    flexDirection: 'row',
    height: 48,
    paddingX: 16,
    width: '100%',
  },
  date: {
    flex: 1,
  },
});

export default styles;
