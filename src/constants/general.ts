import { Platform } from 'react-native';

export const TOTAL_ACCOUNT_SETUP_STEPS = 2;
export const DEFAULT_DISTANCE_FROM_TEXT = 20;
export const IS_IOS = Platform.OS === 'ios';
export const NETWORK_ERROR = 'network error';
